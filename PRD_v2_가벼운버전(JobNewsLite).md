# PRD v2: 채용뉴스 파인더 (JobNewsLite) — 가벼운 버전

버전 2.0 | 작성일 2026-09-14 | v1(`PRD_채용정보_자동알림시스템.md`, 2026-09-13)을 대체

---

## 1. 배경 및 목표

어제(2026-09-13) 만든 v1은 워크넷 스크래핑 + Gmail OAuth2 자동 발송 + GitHub Actions 매일
스케줄이라는, 운영 서비스에 가까운 무거운 구조였습니다. 오늘 N036(뉴스레터 에이전트) 강의를
챕터 8까지 직접 실습해보면서, 채용정보처럼 **건수가 많지 않고 매일 자동으로 돌 필요는 없는
경우**엔 다음 세 가지를 덜어내는 게 더 실용적이라는 결론에 이르렀습니다.

- 다중 소스 스크래핑(워크넷·나라일터·하이브레인넷) → **뉴스/웹 검색 한 가지**로 통일
- Gmail 자동 발송 + 매일 예약 실행 → **바탕화면 아이콘 더블클릭 → 로컬 리포트**로 단순화
- SQLite 이력/중복 제거 DB → **매번 새로 조회**(건수가 적어 중복 관리 자체가 불필요)

**성공 기준(Definition of Done)**: 바탕화면 아이콘을 더블클릭하면, 별도 서버·이메일 인증·
스케줄러 설정 없이 몇 분 안에 조건에 맞는 채용정보가 정리된 리포트가 브라우저에 자동으로 열린다.

## 2. v1 대비 무엇을 가볍게 했는가

| 항목 | v1 (어제) | v2 (오늘, 이 PRD) |
|---|---|---|
| 실행 방식 | GitHub Actions 매일 자동 스케줄 | 바탕화면 `.bat` 더블클릭, 필요할 때만 수동 실행 |
| 데이터 수집 | 워크넷 스크래핑 + 나라일터·하이브레인넷 등 다중 소스(예정) | Claude Agent SDK `WebSearch`로 뉴스·웹 검색 한 가지 |
| 결과 전달 | Gmail API(OAuth2) 자동 발송 | 로컬 HTML 리포트 생성 → 브라우저 자동 오픈(발송 없음) |
| 중복 제거 | SQLite 이력 DB | 없음 — 수동 실행이라 "지금 조건에 맞는 것"만 보면 충분 |
| 필요한 인증정보 | Gmail OAuth 시크릿 + 워크넷 API 키 + Claude API 키 | Claude Agent SDK 하나(card-news-agent와 동일 환경 재사용) |
| 예상 구축 시간 | 1~2일(Phase 1) + 지속 유지보수(스크래핑 깨짐 대응 등) | 반나절 이내 |

## 3. 오늘 배운 파이프라인을 어떻게 적용했는가

N036의 5단계(수집→선별→요약→검수→발행)를 채용정보에 맞게 그대로 재해석했습니다.

1. **수집(Collect)** — RSS나 공식 채용 API 대신, Claude Agent SDK의 `WebSearch`로 "센터장
   콘텐츠 채용", "상무 신사업기획 채용" 같은 DJ님 조건에 맞는 검색어를 던져 최근 채용 관련
   뉴스·공고를 찾습니다. card-news-agent에서 이미 검증된 방식과 동일해서 새 API 키가
   필요 없습니다.
2. **선별(Gate → Score)**
   - **관문(Gate)**: 실제로 `WebFetch`로 열어서 읽을 수 있는 공고인가(G1) · 게시일이 최근인가(G2) ·
     접근 가능한가(G3) — N036 챕터 4·5와 동일한 3관문을 그대로 씁니다.
   - **등급(Score)**: 관문을 통과한 후보들 중 DJ님 경력(콘텐츠·신사업·28년 경력)과의 관련도를
     점수화합니다. 채용정보가 워낙 많지 않아 예선/본선으로 나눌 필요 없이, N036 6장의
     "③ 전체 비교" 방식(후보 수가 적을 때 맞는 선택)을 그대로 씁니다.
3. **요약(Draft)** — 출력 칸을 검증 가능성 기준으로 나눕니다: 기관/기업명·직급·마감일·링크는
   원문과 대조 가능한 칸, 추천 이유는 해석성 칸 — N036 챕터 8과 정확히 같은 원칙입니다.
4. **검수(Verify)**
   - 링크가 실제로 열리는지 다시 확인(G1 재확인)
   - 헤드라인(공고 제목)·추천 이유가 한국어로 작성됐는지 **필드별로 따로** 코드 검사 — 오늘
     card-news-agent에 실제로 적용한 것과 동일한 패턴입니다(`headline+body`를 합쳐서
     검사하면 한쪽 필드의 실패를 가릴 수 있다는 걸 실습으로 확인했기 때문).
   - 사원/대리급 등 제외 키워드가 실수로 포함되지 않았는지 확인
5. **발행(Publish)** — 이메일 대신 로컬 HTML 리포트 파일을 만들고, 스크립트가 끝나면 자동으로
   기본 브라우저에서 그 파일을 엽니다(`webbrowser.open()`).

## 4. 화면 구성 (전체 UX)

1. 바탕화면의 아이콘(`.bat` 파일) 더블클릭
2. 콘솔 창이 뜨며 진행 상황이 실시간으로 출력됨
   ```
   [1/4] 채용 뉴스 검색 중...
   [2/4] 관문(Gate) 통과 확인 중... 12건 중 7건 통과
   [3/4] 관련도 평가 및 추천 이유 작성 중...
   [4/4] 리포트 생성 완료 — 브라우저를 엽니다
   ```
3. 완료되면 자동으로 브라우저에 리포트 HTML이 열림(별도 클릭 불필요)
4. 조건에 맞는 공고가 0건이면 리포트에 "오늘은 조건에 맞는 신규 공고가 없습니다"라고 명확히
   표시(빈 화면·빈 리포트 방지)

## 5. 타겟 유저

DJ 대표님 1인. v1과 동일한 조건을 그대로 유지합니다.
- 정부·공공·대학: 콘텐츠 산업(방송·게임·웹툰·K-컬처), 신규사업 기획/운영 관련 기관 / 센터장·본부장·팀장·책임연구원·전문위원
- 일반기업: 콘텐츠/신사업/전략기획 분야 C-Level, 임원급(이사/상무/전무), 본부장/총괄

## 6. 도구 계약 (DJ의 도구 설계 체크리스트 반영)

| 도구 | 언제 쓰는가 | 언제 안 쓰는가 | 실패 시 |
|---|---|---|---|
| `WebSearch` | 채용 관련 뉴스·공고 후보를 찾을 때만 | 이미 찾은 공고의 상세를 다시 확인할 땐 `WebFetch`를 씀(중복 검색 금지) | 검색 결과 0건이면 검색어를 넓혀 1회만 재시도, 그래도 0건이면 "오늘은 없음"으로 정상 종료(에러 아님) |
| `WebFetch` | 후보 공고 원문을 실제로 열어 G1 관문(본문 확인)을 확인할 때만 | 같은 세션에서 이미 확인한 URL은 재요청 안 함 | 열리지 않으면 그 후보만 관문 탈락, 나머지 실행은 계속(DJ 체크리스트 7번 — 일부 실패해도 전부 버리지 않음) |

- **확인 절차 불필요**: 이메일을 보내지 않고 로컬 리포트만 만들기 때문에, 비용도 적고 되돌리기도
  쉬워 별도 승인 절차를 두지 않습니다(DJ 체크리스트 4번 — 비싸거나 되돌리기 어려운 행동에만
  확인을 받는다는 원칙에 따라, 이 경우는 확인이 필요 없다고 판단).
- **근거 요구**: 추천 이유는 반드시 실제로 확인한 내용(confirmed_facts)에만 근거해야 하며,
  확인 안 된 내용은 추천 이유에서 제외합니다 — card-news-agent의 confirmed_facts/
  unconfirmed_claims 분리 패턴을 그대로 재사용합니다(DJ 체크리스트 5번).

## 7. 필터링 규칙 (v1 6장을 Gate/Score로 재구성)

1. **1차 키워드 게이트(Gate)**: 제목/본문에 "센터장|본부장|팀장|책임연구원|전문위원|이사|상무|전무|
   C-Level|총괄" 등 포함 AND "콘텐츠|웹툰|게임|방송|K-컬처|신사업|전략기획" 등 포함
2. **2차 제외 게이트(Gate)**: "사원|대리|주임|인턴|계약직(단순 보조)" 포함 시 제외, "재무|생산관리|
   단순회계" 등 무관 직무 키워드 포함 시 제외
3. **3차 관련도 평가(Score)**: 1~2차를 통과한 공고에 대해 DJ님 경력 요약을 근거로 관련도 점수
   (0~100)와 추천 이유를 생성. 임계치(예: 60점) 이상만 최종 리포트에 포함, 점수 내림차순으로
   정렬해 최상단에 "오늘의 추천 TOP 1" 표시

## 8. 기술 스택

- **실행 엔진**: Claude Agent SDK — card-news-agent와 동일한 환경을 그대로 재사용(새 인증 불필요)
- **언어**: Python 3.11+
- **리포트**: 정적 HTML 1개 파일 — `강의정리/lecture-assets`의 콜아웃·표 스타일을 재사용 가능
  (이미 여러 강의노트·리포트에서 검증된 디자인)
- **트리거**: Windows `.bat` 파일(바탕화면에 배치) → `python src/main.py` 실행 → 완료 시
  `webbrowser.open()`으로 결과 HTML을 자동으로 엶
- **이력 관리**: 없음(v1 대비 의도적으로 제거). 나중에 필요해지면 JSON 파일 하나로 가볍게
  추가 가능(SQLite 같은 DB는 이 규모에 과함)

## 9. 에러 처리

- 검색 자체가 실패하면(네트워크 등) 1회만 재시도, 그래도 실패하면 리포트에 "검색에 실패했습니다.
  잠시 후 다시 실행해주세요"라고 명확히 표시하고 종료(추측으로 빈 결과를 지어내지 않음)
- 후보 하나의 본문 확인(G1)이 실패해도 그 후보만 제외하고 나머지는 계속 처리

## 10. 단계별 로드맵

- **Phase 1 (이 PRD의 전부, 반나절 목표)**: WebSearch 기반 수집 + Gate/Score 선별 + HTML
  리포트 생성 + `.bat` 더블클릭 실행
- **Phase 2 (선택, 필요해지면)**: "이미 본 공고" 표시만 가벼운 JSON 파일로 추가. 자동 발송·
  스케줄링은 이 버전의 목표가 아니므로 계획에 없음(필요해지면 그때 다시 판단)

## 11. v1과의 관계

이 PRD는 v1(`PRD_채용정보_자동알림시스템.md`)을 대체합니다. v1에서 이미 작성한
`src/filters/rules.py`(1~2차 키워드 필터)는 그대로 재사용 가능하고, `src/reporter/
email_template.py`의 출력 항목 구성(기관명/제목/직급/마감일/추천이유/링크)도 참고할 수
있습니다. 다만 `src/collectors/worknet.py`(스크래핑)·`src/mailer/gmail_sender.py`(발송)·
`src/db.py`(이력 DB)는 이 버전에서 쓰지 않습니다.

---
*이 PRD는 전대진 대표님의 요청으로, N036 강의 챕터8 실습(2026-09-14) 직후 작성되었습니다.*
