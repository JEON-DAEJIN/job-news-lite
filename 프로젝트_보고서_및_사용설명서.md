# JobNewsLite — 프로젝트 보고서 및 사용설명서

> 이 문서는 아이펠 과제 제출용 `REPORT.md`와는 별개로, **원래 하려던 프로젝트 자체**의
> 계획·완성도·실사용 방법을 정리한 문서입니다. 과제 형식(LangGraph 예시, 소스 3개 요건
> 등)에 맞춘 설명은 담지 않고, "DJ 대표님이 실제로 이 도구를 쓸 때 필요한 것"에만
> 집중합니다.

---

## 1부. 프로젝트 계획과 완성도 보고

### 1.1 원래 목표 (PRD v2 기준)

`PRD_v2_가벼운버전(JobNewsLite).md`에서 정의한 성공 기준(DoD):

> 바탕화면 아이콘을 더블클릭하면, 별도 서버·이메일 인증·스케줄러 설정 없이 몇 분 안에
> 조건에 맞는 채용정보가 정리된 리포트가 브라우저에 자동으로 열린다.

v1(어제, 워크넷 스크래핑 + Gmail 자동발송)이 채용정보 건수에 비해 과한 구조였다는
판단에서, "가볍게" 다시 설계한 버전입니다.

### 1.2 계획 대비 구현 현황

| PRD v2 10장 로드맵 항목 | 상태 | 비고 |
|---|---|---|
| WebSearch 기반 수집 | ✅ 완료 | 3트랙(공공기관/채용플랫폼/인사뉴스) 분리까지 완료 — 원래 계획보다 더 세분화됨 |
| Gate/Score 선별 | ✅ 완료 | 키워드 게이트(`filters/rules.py`) + 관련도 점수(`graph.stage_summarize`) |
| HTML 리포트 생성 | ✅ 완료 | `report.py` + `assemble-html.js` |
| `.bat` 더블클릭 실행 | ✅ 완료 | [`run.bat`](./run.bat) |
| 바탕화면 바로가기 배치 | ✅ 완료 | "채용뉴스 파인더" 바로가기가 바탕화면에 있습니다(`run.bat` 연결) |
| 실제 조건으로 end-to-end 1회 실행 검증 | ✅ 완료 | 2026-09-15 11:47 실행, `store/metrics.jsonl` 기록 남음 |
| (Phase 2, 선택) "이미 본 공고" JSON 이력 | ⬜ 미착수 | PRD상 필요해지면 나중에 추가하기로 한 항목, 계획대로 보류 중 |

**PRD v2의 Phase 1 로드맵은 전부 완료됐습니다.**

### 1.3 아키텍처 한눈에 보기

```
audience.yaml  ──┐  (조건 설정 파일 — 여기만 고치면 됨)
                 │
run.bat → run.py │  (실행 진입점 — 콘솔 진행상황 출력 + 기록 저장)
                 │
              src/graph.py     (5단계 파이프라인 로직)
              ├─ stage_collect     : WebSearch/WebFetch로 3트랙 수집
              ├─ dedup_candidates  : 중복 URL 제거
              ├─ stage_select      : 키워드 게이트
              ├─ stage_summarize   : 관련도 점수 + 추천이유
              ├─ stage_verify      : 마감일 검증 + 한국어 검사
              └─ stage_publish     : HTML 리포트 생성
                 │
              data/report_latest.html  (결과 — 브라우저 자동 오픈)
              store/metrics.jsonl      (실행 기록 — 나중에 "몇 건이나 걸렀는지" 확인용)
```

### 1.4 완성도 평가 — 솔직한 한계

1. **표본이 아직 1회, 4건뿐입니다.** 게이트 키워드·점수 임계치(60점)·마감일 정규식이
   "적절한 값"인지는 이번 한 번의 실행만으로 증명되지 않습니다. 며칠 반복 실행해봐야
   신뢰도가 쌓입니다.
2. **`public_agency`(정부·공공기관) 트랙이 이번엔 0건이었습니다.** 워크넷 오픈 API가
   아직 승인 대기 상태라 WebSearch만으로 공공기관 채용 공고를 잡기 어려웠을 가능성이
   있습니다 — 이 트랙의 실효성은 아직 검증되지 않았습니다.
3. **마감일 판정이 정규식 기반**(`graph._is_past_deadline`)이라, "상시모집"·"채용시" 같은
   표현이나 비표준 날짜 형식은 그냥 통과시킵니다(안전하게 보수적으로 처리 — 놓치는 것보다
   더 보여주는 쪽을 택함).
4. **중복 제거가 URL 완전 일치만 잡습니다.** 같은 채용 건을 다른 사이트(예: 사람인과
   잡코리아)가 각각 다루면 두 번 나올 수 있습니다.
5. **이 도구는 이 대화(Claude Code CLI)와 같은 계정 사용량을 나눠 씁니다** — 아주 중요한
   실사용 제약이라 2부(사용설명서)에서 자세히 설명합니다.

**총평**: MVP로서는 완성됐고, 실제 조건으로 최소 1회 정직하게 검증됐습니다. 다만 "매일
믿고 쓸 수 있는 수준"이 되려면 며칠간 반복 실행하며 게이트 키워드·임계치를 다듬는 튜닝
기간이 필요합니다.

### 1.5 바탕화면 바로가기

2026-09-15 기준 바탕화면에 **"채용뉴스 파인더"** 바로가기가 생성돼 있습니다(`run.bat` 연결,
작업 폴더는 프로젝트 루트로 지정됨). 더블클릭하면 바로 실행됩니다 — 2부 2.2절 참고.

---

## 2부. 사용설명서 (매뉴얼)

### 2.1 실행 전에 딱 한 가지만 기억하세요

**이 도구는 지금 저와 나누고 있는 이 Claude Code 대화(세션)와 같은 계정 사용량을 나눠
씁니다.** `run.bat`이 하는 검색·검수 작업은 내부적으로 Claude Code CLI를 통해 실행되기
때문입니다. 그래서:

- 저와 대화를 많이 나눈 직후이거나, 다른 무거운 작업을 방금 시켰다면 `run.bat` 실행이
  "session limit" 오류로 실패할 수 있습니다.
- 이 오류가 나면 콘솔에 `resets 7:10pm (Asia/Seoul)`처럼 리셋 시각이 함께 표시됩니다 —
  그 시각 이후에 다시 실행하면 됩니다. (실제로 이번 프로젝트 개발 중에도 한 번 겪었고,
  다음날 정상적으로 풀렸습니다.)
- 급하지 않다면, 하루에 한두 번(예: 출근길·점심시간) 정도로 실행 빈도를 낮추는 것을
  권합니다 — 어차피 채용정보는 매일 급변하지 않습니다.

### 2.2 실행 방법

**바탕화면 아이콘으로 실행 (권장)**
1. 바탕화면의 "채용뉴스 파인더" 바로가기를 더블클릭
2. 검은 콘솔 창이 뜨면서 진행 상황이 순서대로 출력됩니다:
   ```
   [1/4] 채용 뉴스 검색 중... (소스 3갈래)
     - [정부·공공기관·대학 채용 공고] 검색 중...
     - [채용 플랫폼 공고] 검색 중...
     - [임원 인사·영입 뉴스] 검색 중...
   [2/4] 관문(Gate) 통과 확인 중... N건 수집 중 M건 통과
   [3/4] 관련도 평가 및 추천 이유(인사이트) 작성 중...
   [4/4] 리포트 생성 완료 — 브라우저를 엽니다
   ```
3. 완료되면 기본 브라우저가 자동으로 열리며 리포트가 표시됩니다.
4. 콘솔 창은 결과를 다 읽었으면 아무 키나 눌러 닫으면 됩니다(`pause` 때문에 자동으로
   닫히지 않습니다 — 진행 로그를 놓치지 않게 하려는 의도입니다).

**폴더에서 직접 실행 (바로가기 만들기 전 임시로)**
1. `C:\Users\ilove\Documents\projects\SearchingJobs\JobNewsLite` 폴더로 이동
2. `run.bat`을 더블클릭 (또는 명령 프롬프트에서 `run.bat` 입력)

**바탕화면 바로가기 직접 만드는 법**
1. 위 폴더에서 `run.bat`을 마우스 오른쪽 클릭 → **바로 가기 만들기**
2. 생긴 바로 가기 파일을 바탕화면으로 드래그(또는 잘라내기 → 바탕화면에 붙여넣기)
3. (선택) 바로 가기 아이콘을 원하는 그림으로 바꾸고 싶으면, 바로 가기 우클릭 →
   속성 → 아이콘 변경

### 2.3 리포트 읽는 법

리포트는 다음 순서로 구성됩니다:

1. **오늘의 추천 TOP 1** — 통과한 후보 중 관련도 점수가 가장 높은 1건을 맨 위에 보여줍니다.
2. **정부기관 / 공공기관 / 대학** 섹션 — `category: public`으로 분류된 후보
3. **일반기업 (임원급 / C-Level)** 섹션 — `category: company`로 분류된 후보

각 후보 카드에 표시되는 항목:

| 항목 | 의미 |
|---|---|
| `[기관/기업명] 제목` | 원문 공고/기사의 제목 그대로(번역·재작성 안 함) |
| 마감일 | 원문에서 확인된 마감일. "미상"이면 원문에 명시가 없었다는 뜻 |
| 요약 | 공고/기사 본문 요약 |
| 추천 이유 | AI가 DJ님 경력 관점에서 왜 주목할 만한지 작성한 해석 — **반드시 확인된 사실(confirmed_facts)에만 근거하도록 강제**돼 있지만, 최종 판단은 직접 원문 링크를 열어 확인하시는 걸 권합니다 |
| 관련도 점수 | 0~100점, 60점 이상만 리포트에 실림 |
| 링크 | 원문 공고/기사 주소 — 지원 여부는 반드시 이 링크에서 직접 확인 |

**"오늘은 조건에 맞는 신규 공고가 없습니다"가 뜨면?** — 실제로 조건에 맞는 게 없었을 수도
있고, 검색 조건이 너무 좁아서일 수도 있습니다. 2.4절에서 검색 기간을 넓히는 법을
안내합니다.

### 2.4 조건 바꾸는 법 — `audience.yaml`

코드를 건드릴 필요 없이, 프로젝트 폴더의 `audience.yaml` 파일만 메모장으로 열어 고치면
됩니다. 저장한 뒤 `run.bat`을 다시 실행하면 바로 반영됩니다.

**검색 기간을 넓히고 싶을 때** (예: 결과가 자주 0건일 때)
```yaml
recency_days: 30   # 이 숫자를 60, 90 등으로 늘리면 더 오래된 공고까지 찾습니다
```

**관심 직급/분야 키워드를 추가하고 싶을 때** (예: "프로듀서"도 보고 싶다면)
```yaml
include_keywords:
  field:
    - 콘텐츠
    - 웹툰
    - 프로듀서   # 이렇게 한 줄 추가
```

**점수 임계치를 낮추고 싶을 때** (더 많은 후보를 보고 싶다면)
```yaml
score_threshold: 60   # 예: 50으로 낮추면 더 많은 후보가 리포트에 실립니다
```

**제외하고 싶은 키워드를 추가하고 싶을 때**
```yaml
exclude_keywords:
  field:
    - 단순회계
    - 생산관리
    - 영업직   # 이렇게 추가
```

### 2.5 문제 해결(트러블슈팅)

| 증상 | 원인/대응 |
|---|---|
| `session limit` 오류 | 2.1절 참고 — Claude 계정 사용량 한도. 표시된 리셋 시각 이후 재실행 |
| 콘솔이 바로 닫히고 아무것도 안 뜸 | `uv`가 설치돼 있는지 확인 (`uv --version`). 안 돼 있으면 [uv 설치 페이지](https://docs.astral.sh/uv/) 참고 |
| 브라우저가 자동으로 안 열림 | `data/report_latest.html` 파일을 직접 더블클릭해서 열어도 됩니다 |
| 매번 "0건"만 나옴 | 2.4절대로 `recency_days`를 늘리거나 `score_threshold`를 낮춰서 시험해보세요. 그래도 0건이면 그 기간엔 정말 조건에 맞는 공고가 없었을 가능성이 큽니다 |
| 실행이 너무 오래 걸림(10분 이상) | 정상입니다 — 3개 소스 트랙을 각각 WebSearch+WebFetch로 순차 확인하기 때문에 5~15분 정도 걸릴 수 있습니다 |
| 리포트에 나온 마감일이 이상함 | 원문에 마감일이 없거나 "상시모집"처럼 특수하게 적혀 있으면 시스템이 놓칠 수 있습니다 — 링크를 직접 열어 확인하세요 |

### 2.6 실행 기록 들여다보기 (선택사항)

`store/metrics.jsonl`을 메모장으로 열면, 실행할 때마다 한 줄씩 기록이 쌓입니다. 예:
"이번엔 몇 건 찾았고, 몇 건이 게이트에서 탈락했고, 몇 건이 마감일 때문에 빠졌는지"를
숫자로 확인할 수 있습니다 — 조건을 튜닝할 때(2.4절) 참고하면 좋습니다.
